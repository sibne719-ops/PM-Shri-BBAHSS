# Periodic Table Explorer (Prediction vs Actual)

This project is a simple single-page web app (`index.html`) that lets you explore selected periodic table elements and compare **predicted** vs **actual** values (atomic mass, density, melting point).

## How to run
